/*
 * @lc app=leetcode.cn id=946 lang=csharp
 *
 * [946] 验证栈序列
 */

// @lc code=start
public class Solution {
    public bool ValidateStackSequences(int[] pushed, int[] popped) {
        List<int> pushList = pushed.ToList();
        List<int> popList = poped.ToList();
        
        Stack<int> rootStack = new Stack<int>();

        int index = 0;
        while (rootStack.Count < pushed.Length)
        {
            
        }

    }

    public bool TestPush()
}
// @lc code=end

